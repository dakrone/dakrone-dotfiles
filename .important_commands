curl -u user:Passw0rd localhost:9200/_rebalance
sudo /etc/init.d/elasticsearch stop
sudo /etc/init.d/elasticsearch shut-down-cluster
sudo /etc/init.d/elasticsearch start
sudo rabbitmqctl list_queues -p production-2010-05-08 messages messages_unacknowledged name | grep safe | sort -k3 | fgrep -v sensu
es health
safectl util repl
tail -n250 /var/log/es.log
ENV=dev kssh -m search_0 "curl -s p.draines.com/diskeph.sh | zsh" | sort -rnk6
curl -s https://raw.github.com/dakrone/dakrone-dotfiles/master/bootstrap | zsh
kex "role:search_2 AND role:elasticsearch_ebs" 'puts "#{node.cloud.public_ipv4} \t #{node.cloud.local_ipv4} \t #{node.ec2.placement_availability_zone}"'
